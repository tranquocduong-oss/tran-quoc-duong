#define _CRT_SECURE_NO_WANRINGS
#include<stdio.h>
#include<math.h>
/* hàm sẽ mô tả công việc hay 1 hành động nào đó
vd: printf, scanf
muốn xây dựng hàm cần xác định 3 yếu tố:
chức năng: dùng để làm gì
input của hàm :hàm cần truyền cái gì
output của hàm:sau khi hoạt động hàm sẽ trả về (return) cái gì
ví dụ1:xây dựng hàm để cộng 2 số nguyên
chức năng: cộng 2 số nguyên
input: 2 số nguyên
output: kết quả là phép cộng - là số nguyên
ví dụ2: xây dựng hàm để tính tổng phần tử của mãng
chức năng:tính tổng
input: 
+param1: địa chỉ bắt đầu của mãng
+param2: số lượng phần tử của mãng
output: kết quả-số nguyên
cú pháp khai báo:
<kiểu dữ liệu output> tên hàm(input);
cú pháp build hàm:
<kiểu dữ liệu output> tên hàm(input)*/ 

/*
*@brief tính tổng
*@param int : số nguyên 1
*@param int : số nguyên 2
*@return_int-kết quả của phép cộng
*/
//int cong_so_nguyen(int so_nguyen_1, int so_nguyen_2);
//int cong_so_nguyen(int so_nguyen_1, int so_nguyen_2) {
	//int kQ = 0;
	//kQ = so_nguyen_1 + so_nguyen_2;
	//return kQ;
//}
//void bai1_buoi5_2() {
	//int x = cong_so_nguyen(1, 2);
	//printf("ket qua: %d", x);
//}





/*
* @brief tinh tong cac phan tu
* @param char*: địa chỉ bắt đầu cảu mảng
*@param int: nhập số lượng phần tử của mảng
*@return int: kết quả
*/


